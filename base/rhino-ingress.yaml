apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rhino-ingress
spec:
  rules:
    - http:
        paths:
        - path: /
          pathType: ImplementationSpecific
          backend:
            service:
              name: dev-rhino-service
              port:
                number: 80